apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: myapp
  labels:
    app: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
