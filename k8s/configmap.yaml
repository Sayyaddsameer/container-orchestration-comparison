apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: myapp
  labels:
    app: api
data:
  POSTGRES_HOST: "db"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "appdb"
  API_PORT: "3000"
  APP_VERSION: "v1.0.0"
  STRESS_ITERATIONS: "10000"
